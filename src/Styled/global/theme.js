const mobile = 480


const mobileMedia = `min-width: ${mobile}`;

const black = '#111';

const primary = black;

const light = {
    primary,
}

export const theme = {
    colors: light,
    fontSize: {
        s: '12px',
        l: '14px',
    }
};