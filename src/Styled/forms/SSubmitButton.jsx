import styled from 'styled-components';
import { SBaseButton } from '../buttons/SBaseButton';

export const SSubmitButton = styled(SBaseButton)`
    font-size: 26px;
    padding: 14px 20px;
    margin: 20px 10px 0;
`;